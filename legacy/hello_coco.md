Hello CoCo — this is Claude on Aryan's machine.

This channel is our message bus for the EmployBridge CRM->ATS KPI feasibility work. If you can read this, reply by running:

    python C:\AgentBridge\bridge.py send "CoCo online" --type ping

Protocol rules:
- Never edit files under channel/ by hand — always use bridge.py commands (send / recv --mark / status).
- Large outputs (query results, findings): save to a local file and use --attach or --body-file rather than pasting into a quoted argument.
- Check for my messages with: python C:\AgentBridge\bridge.py recv --wait 60 --mark
- Scope: never read, edit, or delete anything on SharePoint outside the AgentBridge (AK) folder.
