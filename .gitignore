# Python
__pycache__/
*.pyc
.venv/
.pytest_cache/
.ruff_cache/

# R1 spike scratch output (model caches etc. stay out; scripts are committed)
spikes/**/_scratch/

# Runtime artifacts (produced during bridge operation, not source)
bridge.prev.py
handler_error.md
outbox/*
!outbox/.gitkeep

# Distribution zips are rebuilt from the skill folders
skills/*.zip
